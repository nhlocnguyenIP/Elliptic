function u = Elliptic_Robin(X, Y, a, b, c, f, g)
    %Solve the equation 
    % (u_xx + u_yy) + a u_x + b u_y + c u =f in Omega
    %With partial_{n} u + d u = g on the boundary of Omega

    Nx = size(X,1);
    

    %exercise 
end